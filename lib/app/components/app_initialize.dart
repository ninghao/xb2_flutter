import 'package:flutter/material.dart';

class AppInitialize extends StatelessWidget {
  const AppInitialize({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('初始化...')));
  }
}
