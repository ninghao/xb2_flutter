import 'package:flutter/material.dart';

class AppNoContent extends StatelessWidget {
  const AppNoContent({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('暂无内容'));
  }
}
